[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gordon-plugin-sdk"
version = "0.1.0"
description = "Official Python SDK for Gordon Autonomous Analytics Platform community plugins"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
